import React from "react";
import DefaultLoader from "@components/loaders/DefaultLoader";
type Props = {};

const SvgAnimation = (props: Props) => {
  return <DefaultLoader />;
};

export default SvgAnimation;
